# 🔧 حل مشكلة "error"

## المشكلة:
إذا ظهرت رسالة "error" أو "node_modules NOT found"، هذا يعني أن المكتبات لم يتم تثبيتها.

## الحل السريع:

### الطريقة 1: استخدام ملف الإعداد (الأسهل)
1. انقر نقراً مزدوجاً على: **`SETUP_AND_RUN.bat`**
2. انتظر حتى يتم تثبيت المكتبات
3. سيُفتح المتصفح تلقائياً

### الطريقة 2: يدوياً

```powershell
# 1. اذهب لمجلد frontend
cd D:\sps\frontend

# 2. ثبت المكتبات
npm install

# 3. شغّل السيرفر
npm run dev

# 4. افتح المتصفح يدوياً على:
# http://localhost:3000/dashboard/owner
```

## خطوات مفصلة:

### 1. تثبيت المكتبات
```bash
cd frontend
npm install
```

هذا قد يستغرق 2-5 دقائق في المرة الأولى.

### 2. تشغيل السيرفر
```bash
npm run dev
```

### 3. فتح المتصفح
- افتح: **http://localhost:3000/dashboard/owner**
- أو شغّل: **`OPEN_BROWSER.bat`**

## إذا استمرت المشكلة:

### تحقق من:
1. ✅ Node.js مثبت (اكتب `node --version` في Terminal)
2. ✅ أنت في المجلد الصحيح (`frontend`)
3. ✅ الإنترنت متصل (لتحميل المكتبات)

### إعادة تثبيت كاملة:
```powershell
cd frontend
Remove-Item -Recurse -Force node_modules -ErrorAction SilentlyContinue
Remove-Item package-lock.json -ErrorAction SilentlyContinue
npm install
npm run dev
```

## الملفات المتاحة:

- **`SETUP_AND_RUN.bat`** - الإعداد والتشغيل التلقائي (استخدم هذا!)
- **`start-with-browser.bat`** - تشغيل مع فتح المتصفح
- **`OPEN_BROWSER.bat`** - فتح المتصفح فقط
- **`RUN_ME.bat`** - تشغيل بسيط

