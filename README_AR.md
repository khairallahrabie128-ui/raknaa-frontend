# 🚀 دليل التشغيل السريع

## المشكلة: "Cannot GET /dashboard/owner"

### الحل السريع:

#### الطريقة 1: استخدام ملف التشغيل (الأسهل)
1. انقر نقراً مزدوجاً على: **`RUN_ME.bat`**
2. انتظر حتى يفتح المتصفح تلقائياً
3. افتح: **http://localhost:3000/dashboard/owner**

#### الطريقة 2: يدوياً

1. **افتح Terminal في مجلد `frontend`**
   ```powershell
   cd D:\sps\frontend
   ```

2. **تثبيت المكتبات** (إذا لم تكن مثبتة)
   ```powershell
   npm install
   ```

3. **امسح الـ cache**
   ```powershell
   Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue
   ```

4. **شغّل السيرفر**
   ```powershell
   npm run dev
   ```

5. **افتح المتصفح**
   - افتح: **http://localhost:3000/dashboard/owner**

## ⚠️ ملاحظات مهمة:

- ✅ تأكد من أنك في مجلد `frontend` وليس `sps`
- ✅ تأكد من أن Node.js مثبت (افتح Terminal واكتب `node --version`)
- ✅ تأكد من أن المنفذ 3000 غير مستخدم
- ✅ إذا ظهرت أخطاء، اقرأ الرسالة في Terminal

## 🔍 إذا استمرت المشكلة:

1. **تحقق من Terminal**: يجب أن ترى رسالة "Ready" من Next.js
2. **تحقق من المتصفح**: اضغط F12 وافتح Console لرؤية الأخطاء
3. **جرب صفحة أخرى**: افتح http://localhost:3000 أولاً للتأكد أن السيرفر يعمل

## 📞 الصفحات المتاحة:

- الرئيسية: http://localhost:3000
- تسجيل الدخول: http://localhost:3000/login
- التسجيل: http://localhost:3000/register
- لوحة المالك: http://localhost:3000/dashboard/owner
- لوحة المستخدم: http://localhost:3000/dashboard/user
- لوحة المسئول: http://localhost:3000/dashboard/admin

